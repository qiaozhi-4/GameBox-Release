# GameBox v1.2.28

## 更新内容

- 完成 `1.2.28`（`versionCode=37`）Release APK 构建与发布清单生成。
- 完成 APK 签名、ABI、文件大小和 SHA-256 一致性检查。
- 完成本版本安装与启动验证。
- 本版本仍保留 APK 内置 `libxmg_core.so`，外置 Native 制品切换将在后续阶段实施。

## 兼容性

- 最低 Android SDK：28。
- 当前支持 ABI：`arm64-v8a`。
- `versionCode`：`37`。


# GameBox Core Native v1.0.0

## 更新内容

- 发布独立的 GameBox Core Native 资产 `libxmg_core-arm64-v8a.so`。
- 提供公共 Native 实现，供 `separate-core` 兼容模式使用。

## 兼容性

- 最低 App `versionCode`：`37`。
- 支持 ABI：`arm64-v8a`。
- 打包模式：`separate-core`；BNM 自包含游戏不依赖该独立 core。

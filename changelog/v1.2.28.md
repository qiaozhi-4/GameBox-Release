# GameBox v1.2.28

## 更新内容

- 完成 `1.2.28`（`versionCode=37`）统一 Release 构建与发布清单生成。
- 完成 APK 签名、ABI、文件大小和 SHA-256 一致性检查。
- 发布 APK 资产 `GameBox-arm64-v8a.apk`。
- 发布 core Native 资产 `libxmg_core-arm64-v8a.so`，与四个游戏 Native 一样作为 `native.games[]` 条目发布。
- 完成本版本安装、启动和 Native 资产验证。

## 兼容性

- 最低 Android SDK：28。
- 当前支持 ABI：`arm64-v8a`。
- `versionCode`：`37`。
- 所有 Native 组件统一使用 `self-contained-game` 加载模式；core 不配置推荐游戏包名。
